# 本版修改说明

这版按“不要改原来格式，但同时做好博客和字体”处理：

1. 首页格式不重做，保留原来的绿色科技风、卡片布局、入口导航、按钮和视觉节奏。
2. 字体继续统一，压低过重字重和过大的标题，避免中文显示别扭。
3. `/blog/` 改成教程优先：中间文章列表为主，右侧放推荐、关键词和最新文章。
4. ZEOGPT 转化保留，但不抢文章主位。
5. npm 兼容处理：不带 `node_modules` 和 `package-lock.json`，保留 `.npmrc` 使用国内镜像。
