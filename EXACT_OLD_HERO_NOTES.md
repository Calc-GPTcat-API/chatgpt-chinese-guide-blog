# 精确旧站首屏标题版

本版只改首页首屏上半部分：

1. 顶部图标已删除。
2. 顶部搜索框已删除。
3. 首页首屏采用旧站图 2 的模板结构。
4. 旧站标题相关 CSS 直接从 `gpt-recharge-service-site.zip` 的 `docs/.vitepress/theme/custom.css` 截取：
   - `.hero-title`
   - `.title-line`
   - `.title-main`
   - `.title-sub`
   - `@keyframes dynamicGradientText`
5. 没有重新猜字号、字重和渐变。
6. 内容已替换成当前站图 1 的 ChatGPT 中文指南 / ZEOGPT 入口信息。
7. 首屏下面的模块没有改。
