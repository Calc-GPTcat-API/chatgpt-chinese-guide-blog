# Above-Fold Conversion Fixes

## What changed

1. Every article now shows the ZEOGPT affiliate link immediately below the H1 title.
2. The ZEOGPT CTA card is placed directly below that visible link, before the intro text and before the table of contents.
3. The previous first CTA that was below the table of contents was removed, so each article still has exactly three CTA components.
4. The audit script now requires the first-screen ZEOGPT link and expects five rendered affiliate hrefs per article:
   - article direct link
   - first-screen CTA
   - mid-article CTA
   - bottom CTA
   - top navigation ZEOGPT registration link

Affiliate URL: https://www.zeogpt.com/register?ref=Ac3KbS3F
